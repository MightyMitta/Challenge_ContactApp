<Window x:Class="Challenge_ContactApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Challenge_ContactApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="768" Width="1024" DataContext="{Binding Source={StaticResource Locator}, Path=Main}" Background="#FF2F378F" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Column="0" Grid.ColumnSpan="3" Background="#FF111761" Foreground="White">
            <MenuItem Header="_File">
                <MenuItem Header="_Close" Background="#FF111761" Command="{Binding CloseApp}"/>
            </MenuItem>
            <MenuItem Header="Contact">
                <MenuItem Header="_New" Background="#FF111761"/>
                <MenuItem Header="_Edit" Background="#FF111761"/>
                <MenuItem Header="_Remove" Background="#FF111761"/>
            </MenuItem>
        </Menu>
        <Frame NavigationUIVisibility="Hidden" Content="{Binding CurrentPage}" Grid.Row="2"/>
    </Grid>
</Window>
